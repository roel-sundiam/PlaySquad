export const environment = {
  production: false,
  apiUrl: 'http://localhost:3001/api',  // Changed to 3001 to avoid conflict with Tennis Club backend
  socketUrl: 'http://localhost:3001'    // Changed to 3001 to avoid conflict with Tennis Club backend
};